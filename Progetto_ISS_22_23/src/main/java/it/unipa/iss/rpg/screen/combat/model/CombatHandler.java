package it.unipa.iss.rpg.screen.combat.model;

/**Interface CombatHandler, provides all actions available during a fight
 * @author Bonura Giovanni*/
public interface CombatHandler {
    void inputAction(int choice);
}
